program T2;
var
    n, i, fat: integer;

begin
    read(n);
    fat := 1;
    i := 1;

    while i <= n do
    begin
        fat := fat * i;
        i := i + 1;
    end;

    if (n <> 0) and (fat > 1) then
        write(fat)
    else
        write(1);
end.

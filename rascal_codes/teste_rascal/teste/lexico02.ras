program erro_caractere_invalido;
var
    a, b : integer;
begin
    read(a, b);
    if a > b then
    begin
        write(a)
    end
    else
    begin
        a := b + ?;
        write(b)
    end
end.
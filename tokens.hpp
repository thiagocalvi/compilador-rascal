#ifndef TOKENS_HPP
#define TOKENS_HPP

enum TokenType {
		// Palavras reservadas da linguagem
		TOKEN_PROGRAM = 256,
		TOKEN_VAR,
		TOKEN_INTEGER,
		TOKEN_BOOLEAN,
		TOKEN_PROCEDURE,
		TOKEN_FUNCTION,
		TOKEN_BEGIN,
		TOKEN_END,
		TOKEN_IF,
};

#endif
